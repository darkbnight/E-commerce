# 商品筛选自动运费测算测试用例

## 页面主链路测试
| 编号 | 用例 | 操作 | 预期 | 执行结果 |
| --- | --- | --- | --- | --- |
| UI-001 | 单品入池后自动展示运费 | 打开 `/results`，点击单行 `加入筛选池`，切到 `商品筛选` | 目标商品已进入筛选工作台，且“运费”卡片不是 `-` | 已执行，通过 |
| UI-002 | 自动运费不阻断阶段推进 | 在已自动带出运费的筛选条目上继续点击 `进入测价 -> 测价通过 -> 已找到货源 -> 竞品已整理 -> 进入商品数据整理` | 阶段主链路保持可用，自动运费不会影响后续操作 | 已执行，通过 |

## API 接口测试
| 编号 | 接口 | 用例 | 预期 | 执行结果 |
| --- | --- | --- | --- | --- |
| API-001 | `POST /api/product-selection/items` | 批量加入两条带尺寸重量的快照 | 返回 200，`insertedCount=2`，返回列表中的 `initialDeliveryCost` 已自动回填 | 已执行，通过 |
| API-002 | `POST /api/product-selection/items` | 重复加入已入池商品 | 返回 200，`duplicateCount` 递增，不新增重复条目 | 已执行，通过 |
| API-003 | `PATCH /api/product-selection/items/:id` | 对已自动带出运费的条目继续执行测价写入 | 返回 200，后续手工测价字段仍可正常覆盖 | 已执行，通过 |

## 数据库存储与写入测试
| 编号 | 表名 | 字段级校验 | 预期 | 执行结果 |
| --- | --- | --- | --- | --- |
| DB-001 | `product_selection_items` | `source_job_id`、`source_snapshot_id`、`initial_delivery_cost` | `source_snapshot_id=2001/2002` 的入池记录成功落库，`source_job_id=11` 正确，`initial_delivery_cost` 为非空正数 | 已执行，通过 |
| DB-002 | `product_selection_items` | `source_job_id`、`source_platform`、`source_platform_product_id` 唯一约束 | 重复加入同一商品时不产生第二条记录 | 已执行，通过 |

## 异常与边界测试
| 编号 | 用例 | 操作 | 预期 | 执行结果 |
| --- | --- | --- | --- | --- |
| ERR-001 | 重复入池去重 | 对同一 `sourceSnapshotId` 重复调用创建接口 | 返回 `duplicateCount`，数据库无重复脏数据 | 已执行，通过 |
| ERR-002 | 自动运费与阶段推进兼容 | 创建后继续执行阶段 PATCH 和流转接口 | 不因自动写入 `initial_delivery_cost` 产生 400 或脏数据 | 已执行，通过 |

## 执行记录
- 技术自测脚本：
  - `node scripts/test-product-selection-api.mjs`
  - `node scripts/test-ecommerce-db-ui.mjs`
- UI 截图回归：本轮未触发 UI 截图回归。原因：本次没有调整页面结构、视觉样式或关键状态页面布局，只新增既有卡片的数据自动回填。
