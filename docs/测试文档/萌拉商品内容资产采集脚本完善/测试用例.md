# 萌拉商品内容资产采集脚本完善 测试用例

## 1. 页面主链路测试
本轮不涉及页面改动，本模块不适用。

## 2. API 接口测试
本轮不新增项目内 API，本模块改为“脚本入口验证”。

| 用例ID | 用例名称 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| SCRIPT-001 | 商品内容资产脚本可成功返回 | 执行 `node scripts/menglar-capture/content-assets.mjs --product-id 2755299450 --json` | 返回 `ok=true`，且包含 `libraryId / contentAssetId / asset.title / asset.skus` | 已执行，通过 |
| SCRIPT-002 | 不存在商品时返回失败 | 执行 `node scripts/menglar-capture/content-assets.mjs --product-id 0000000000 --json` | 脚本失败并提示“采集箱中未找到商品” | 已执行，通过 |

## 3. 数据库存储与写入测试

| 用例ID | 表名 | 核验字段 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- | --- |
| DB-001 | `product_content_assets` | `platform_product_id`、`title`、`description`、`tags_json`、`image_urls_json`、`content_hash` | 成功执行内容资产脚本后查库 | `platform_product_id=2755299450`，标题和描述非空，`tags_json` 为 JSON 数组，`content_hash` 非空 | 已执行，通过 |
| DB-002 | `product_content_skus` | `content_asset_id`、`platform_product_id`、`platform_sku_id`、`sku_name`、`images_json` | 根据最新 `product_content_assets.id` 查询关联 SKU | 至少 1 条 SKU，且 `content_asset_id` 正确关联，`images_json` 为 JSON 数组 | 已执行，通过 |
| DB-003 | `source_jobs` | `page_type`、`job_status`、`record_count`、`error_message` | 查询本次脚本生成的最新任务记录 | 成功场景下 `page_type=product_content_assets`、`job_status=success`、`record_count=1` | 已执行，通过 |

## 4. 异常与边界测试

| 用例ID | 用例名称 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| ERR-001 | 重复内容不重复入库 | 执行 `node scripts/test-menglar-content-assets.mjs` | 同一 `content_hash` 只保留 1 条内容资产记录，SKU 不重复插入 | 已执行，通过 |
| ERR-002 | 缺少商品 ID 时阻断 | 执行 `node scripts/menglar-capture/content-assets.mjs --json` | 直接报错“必须提供 --product-id” | 已执行，通过 |

## 5. 执行记录
- UI 截图回归：本轮未触发 UI 截图回归。
- 自动化命令：
  - `node scripts/test-menglar-content-assets.mjs`
  - `node scripts/menglar-capture/content-assets.mjs --product-id 2755299450 --json`
  - `node scripts/menglar-capture/content-assets.mjs --product-id 0000000000 --json`
