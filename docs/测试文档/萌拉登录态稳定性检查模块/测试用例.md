# 萌拉登录态稳定性检查模块测试用例

> 说明：仓库中未找到 `docs/测试文档/测试用例书写规则.md`，本文件按现有测试文档目录结构记录本轮自测范围。

## 脚本主链路测试

- [x] 执行 `node --check scripts/menglar-capture/lib/login-health.mjs`，语法检查通过。
- [x] 执行 `node --check scripts/menglar-capture/login-health.mjs`，语法检查通过。
- [x] 执行 `node --check scripts/menglar-capture/preflight.mjs`，语法检查通过。
- [x] 执行 `node scripts/menglar-capture/login-health.mjs --target hot_products --json`，返回标准 JSON。

## 登录态判断测试

- [x] 当前 profile 可读取本地登录缓存时，输出 `storage.runtimeStorageLoaded=true`。
- [x] 当前页面检测为游客态时，输出 `ok=false`、`errorType=guest_blocked`。
- [x] 当前无可采业务接口授权时，输出 `api.authorizedRequestCount=0`。
- [x] 检查结果写入 `.cache/menglar-capture/login-health-last.json`。

## Preflight 兼容测试

- [x] 执行 `node scripts/menglar-capture/preflight.mjs --target hot_products --json`，输出仍包含原有 `auth.runtimeStorageLoaded`、`auth.authorizationCaptured`、`errorType`、`nextAction`。
- [x] `preflight` 能复用登录态模块，并在当前不可采状态下返回 `ok=false`。

## 页面与 API 测试

- [x] 请求 `POST /api/menglar/login-health` 返回 200，结构包含 `ok`、`status`、`api`、`nextAction`。
- [x] 数据采集页展示“采集可用性检查”卡片。
- [x] 点击“检查是否可采”后展示检查结果。
- [ ] 点击“刷新 Profile 后检查”后传入 `refresh=true` 并展示检查结果。
- [x] UI 截图回归覆盖默认态和检查结果态。

## 未覆盖项

- [ ] 未构造真实 `api_unauthorized=401` 可重复样本；该分支通过代码路径覆盖，后续遇到 401 时会落入 `api_unauthorized`。
- [ ] 未构造 `ok=true` 样本；需要用户完成稳定登录并关闭紫鸟窗口让 profile 落盘后再次执行。
- [ ] “刷新 Profile 后检查”按钮未单独点击截图，接口参数路径已接入前端。
