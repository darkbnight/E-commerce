# 萌拉工作台目录重构测试用例

## 1. 页面主链路测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| UI-01 | 任务页访问 | 1. 执行 `npm run workbench:web:build` 2. 执行 `npm run workbench:api` 3. 访问 `http://127.0.0.1:4186/tasks` | 返回 200，页面正常加载 | 已通过 |
| UI-02 | 结果页访问 | 1. 执行 `npm run workbench:web:build` 2. 执行 `npm run workbench:api` 3. 访问 `http://127.0.0.1:4186/results` | 返回 200，页面正常加载 | 已通过 |

## 2. API 接口测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| API-01 | 任务接口读取 | 启动 `npm run workbench:api` 后请求 `GET /api/jobs` | 返回 200，包含任务列表 | 已通过 |
| API-02 | 商品接口读取 | 启动 `npm run workbench:api` 后请求 `GET /api/products` | 返回 200，包含 `latestJob`、`summary`、`items` | 已通过 |

## 3. 数据库存储与写入测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| DB-01 | 数据库主路径切换 | 执行 `npm run mvp:db:inspect` | 成功读取 `db/menglar-mvp.sqlite` | 已通过 |
| DB-02 | 关键字段校验 | 查 `products_normalized` 表 | `job_id=11` 下存在 `ozon_product_id=2107989735`、`brand='Лилия'`、`sales=47689` | 已通过 |

## 4. 异常与边界测试

| 编号 | 测试项 | 操作步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| EX-01 | 旧目录移除后服务可用 | 删除旧 `tools/menglar-data-viewer/server.mjs` 后重新执行 `npm run workbench:api` | 服务仍可正常启动 | 已通过 |
| EX-02 | 前端静态托管路径变更 | 构建后访问 `/results` | 页面资源从 `frontend/menglar-workbench/dist` 返回 | 已通过 |

## 5. 执行记录
- 本轮未触发 UI 截图回归。原因：没有页面布局、样式、交互层改动，仅发生目录与服务入口迁移。
