# 萌啦商品内容资产采集技能 测试用例

## 1. 页面主链路测试

本轮不涉及前端页面改动，本模块不适用。

## 2. API 接口测试

本轮不新增项目 API，本模块改为“技能触发与脚本入口校验”。

| 用例ID | 用例名称 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| SKILL-001 | 技能结构校验通过 | 执行 `python C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\menglar-product-content-assets` | 校验通过，无 frontmatter、命名或目录结构错误 | 已执行，通过 |
| SKILL-002 | 技能正文包含闭环流程 | 打开 `SKILL.md` 检查正文 | 明确包含“登录态检查 -> 内容资产采集 -> 数据库核验 -> 结果输出” | 已执行，通过 |
| SKILL-003 | 技能元数据可被正确读取 | 打开 `agents/openai.yaml` 检查字段 | `display_name`、`short_description`、`default_prompt` 存在且 `default_prompt` 正确引用 `$menglar-product-content-assets` | 已执行，通过 |

## 3. 数据库存储与写入测试

本轮不新增数据库写入逻辑，但技能必须明确后续执行时的数据库核验口径。

| 用例ID | 表名 | 核验字段 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- | --- |
| DB-001 | `source_jobs` | `page_type`、`job_status`、`record_count` | 检查 `references/fields-and-validation.md` | 文档明确要求校验成功任务记录，并写出字段名 | 已执行，通过 |
| DB-002 | `product_content_assets` | `id`、`source_job_id`、`platform_product_id`、`content_hash`、`captured_at` | 检查 `references/fields-and-validation.md` | 文档明确要求后续 AI 直连查库验证主表主键、关联键和关键字段 | 已执行，通过 |
| DB-003 | `product_content_skus` | `content_asset_id`、`platform_product_id`、`platform_sku_id`、`images_json` | 检查 `references/fields-and-validation.md` | 文档明确要求核验 SKU 资产表与内容版本的关联关系 | 已执行，通过 |

## 4. 异常与边界测试

| 用例ID | 用例名称 | 步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| ERR-001 | 未登录时先排查而不是误判脚本失败 | 检查 `SKILL.md` 与 `references/workflow.md` | 明确要求先跑 `login-health.mjs`，并区分 `login_required / guest_blocked / api_auth_missing / api_unauthorized` | 已执行，通过 |
| ERR-002 | 页面可见但接口不可用时不误判 | 检查 `SKILL.md` 边界说明 | 明确写出“页面能打开不等于接口可用，必须以授权请求和接口返回为准” | 已执行，通过 |
| ERR-003 | 标题字段异常时有正确归因 | 检查 `references/fields-and-validation.md` | 明确说明 `offerName` 可能直接返回商品 ID，不应直接归因于脚本缺陷 | 已执行，通过 |

## 5. 执行记录

- UI 截图回归：本轮未触发 UI 截图回归。
- 已执行命令：
  - `python C:\Users\Administrator\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\menglar-product-content-assets`
