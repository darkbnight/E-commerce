# 萌拉采集结果展示页测试用例

## 页面主链路测试

| 用例ID | 用例名称 | 操作步骤 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| UI-001 | 页面默认加载最新任务 | 启动本地服务并打开首页 | 页面显示最新任务摘要和商品表格 | 已执行，通过 |
| UI-002 | 关键词筛选 | 输入 `Zewa` 并点击应用筛选 | 列表仅展示匹配品牌/类目的记录 | 已执行，通过 |
| UI-003 | 最低销量筛选 | 输入最低销量 `20000` 并应用 | 列表只保留销量大于等于 20000 的商品 | 已执行，通过 |
| UI-004 | 空态展示 | 输入无匹配关键词 | 表格显示空态提示 | 已执行，通过 |

## API 接口测试

| 用例ID | 接口 | 验证点 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| API-001 | `GET /api/jobs` | 任务列表返回 | 返回最近任务数组 | 已执行，通过 |
| API-002 | `GET /api/products` | 默认任务读取 | 返回最新成功任务商品数据 | 已执行，通过 |
| API-003 | `GET /api/products?keyword=Zewa` | 关键词筛选 | 返回结果包含 `brand='Zewa'` | 已执行，通过 |
| API-004 | `GET /api/products?minSales=20000` | 数值筛选 | 返回结果 `sales >= 20000` | 已执行，通过 |

## 数据库存储与写入测试

| 用例ID | 表名 | 字段级验证 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| DB-001 | `source_jobs` | `id=11` 的 `job_status='success'` | 页面默认读取最新成功任务 | 已执行，通过 |
| DB-002 | `products_normalized` | `job_id=11` 下存在 `ozon_product_id='2107989735'` | 页面可展示有效商品样本 | 已执行，通过 |
| DB-003 | `products_normalized` | `brand='Zewa'`、`sales>=20000` 的记录可被接口正确筛出 | 筛选结果与查库一致 | 已执行，通过 |

## 异常与边界测试

| 用例ID | 用例名称 | 验证点 | 预期结果 | 执行结果 |
| --- | --- | --- | --- | --- |
| ERR-001 | 数据库不存在 | 启动服务时无 SQLite | 接口返回空列表而非服务崩溃 | 未执行 |
| ERR-002 | 空筛选结果 | 条件无匹配 | 页面显示空态文案 | 已执行，通过 |

## UI截图
- [viewer-default.png](G:\work\wangziqiang\E-commerce\docs\测试文档\萌拉采集结果展示页\UI\viewer-default.png)
- [viewer-keyword.png](G:\work\wangziqiang\E-commerce\docs\测试文档\萌拉采集结果展示页\UI\viewer-keyword.png)
- [viewer-empty.png](G:\work\wangziqiang\E-commerce\docs\测试文档\萌拉采集结果展示页\UI\viewer-empty.png)
