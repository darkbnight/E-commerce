# Ozon本地快速定价前端化测试用例

## 页面主链路测试

| 用例 | 步骤 | 预期 | 执行结果 |
| --- | --- | --- | --- |
| 默认页面加载 | 打开 `/ozon-pricing` | 页面展示定价参数、物流方式下拉、计算结果、收入和成本拆分 | 通过 |
| 物流计算器结果接入 | 输入重量和长宽高 | 境外物流方式下拉展示 `/api/shipping/compare` 返回的物流服务和费用 | 通过 |
| 输入触发重算 | 修改采购成本、包装重量、前台折扣 | 物流服务重新计算，折后售价结果发生变化，页面不报错 | 通过 |
| 物流费用对比 | 点击 `查看物流费用对比` | 弹窗展示物流计算器服务、费用、计费重、实重、体积重和时效 | 通过 |

## API 接口测试

| 用例 | 步骤 | 预期 | 执行结果 |
| --- | --- | --- | --- |
| 复用物流接口 | 页面输入重量和尺寸后触发请求 | 调用既有 `POST /api/shipping/compare`，不新增 API | 通过 |

## 数据库存储与写入测试

| 用例 | 步骤 | 预期 | 执行结果 |
| --- | --- | --- | --- |
| 数据库影响检查 | 核对实现范围和代码路径 | 本轮不写数据库，不新增表和字段，数据库字段级验证不适用 | 通过 |

## 计算逻辑测试

| 用例 | 步骤 | 预期 | 执行结果 |
| --- | --- | --- | --- |
| 默认定价 | 执行 `node scripts/Ozon本地快速定价前端化/pricing-calculator-test.mjs` | 折后美元售价、卢布售价、利润、利润率、计费重量均为有限值 | 通过 |
| 手动覆盖 | 同一脚本设置手动物流费和手动佣金率 | 结果优先使用手动覆盖值 | 通过 |
| 异常费率 | 同一脚本设置过高费率和毛利率 | 不输出 `NaN`，返回风险提示 | 通过 |

## 异常与边界测试

| 用例 | 步骤 | 预期 | 执行结果 |
| --- | --- | --- | --- |
| 过高费率边界 | 毛利率、广告费、提现费和退货费组合超过可定价区间 | 售价归零并展示风险提示 | 通过 |
| 手动覆盖为空 | 清空手动物流费和手动佣金率 | 自动使用物流计算器选中服务和类目佣金快照计算 | 通过 |
| 物流接口失败兜底 | 物流计算器接口不可用 | 页面提示可填写手动物流费继续估算 | 未单独模拟，保留人工检查项 |

## UI 截图回归

- 截图目录：`docs/测试文档/Ozon本地快速定价前端化/UI/`
- 覆盖状态：
  - `ozon-pricing-default.png`：默认态
  - `ozon-pricing-result.png`：输入变更后的结果态
  - `ozon-pricing-compare.png`：物流费用对比弹窗态
- 截图用途：本地人工 UI 回归留档。

## 执行命令

```bash
node scripts/Ozon本地快速定价前端化/pricing-calculator-test.mjs
npm.cmd run workbench:web:build
node scripts/Ozon本地快速定价前端化/page-e2e.mjs
```

## 执行结论

✅ 已通过：计算脚本、前端构建、页面 E2E 和 UI 截图回归均完成。
